#!/bin/bash

# 0. 패키지 명단 최신화 (가장 먼저 실행)
# 가상환경 활성화 상태에서 실행됨을 가정합니다.
pip freeze > requirements.txt

# 1. 변경된 모든 파일 추가 (새로 작성된 requirements.txt 포함)
git add .

# 2. 현재 시간으로 커밋 메시지 자동 생성
git commit -m "Daily update: $(date +'%Y-%m-%d %H:%M')"

# 3. 서버로 전송
git push origin main

echo "📦 패키지 목록 갱신 및 🚀 GitHub 업로드 완료!"
